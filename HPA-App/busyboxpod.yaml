apiVersion: v1
kind: Pod

metadata:
  name: busybox-pod

spec:
  restartPolicy: Never

  containers:
  - name: busybox-container
    image: busybox

    command:
    - sh
    - -c
    - |
      while true; do
        wget -q -O- http://hpa-app-service
      done